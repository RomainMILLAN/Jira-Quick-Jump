# Store submission

Copy-paste material for the Chrome Web Store and AMO, plus the permission
justifications a reviewer will ask for. Keep this in step with `src/manifest.json`.

## Metadata

| Field | Value |
|---|---|
| Name | Quick Jump for Jira |
| Summary (≤132 chars) | Type an issue key in the address bar and land straight on the Jira issue. No keyword, no results page. |
| Category | Productivity / Developer Tools |
| Language | English |
| Homepage | https://github.com/RomainMILLAN/Jira-Quick-Jump |
| Privacy policy | https://github.com/RomainMILLAN/Jira-Quick-Jump/blob/main/PRIVACY.md |

*Jira is a trademark of Atlassian. This extension is not affiliated with, endorsed
by, or produced by Atlassian.*

## Description

```
Type ABC-1234 in the address bar. Land on the issue.

No keyword to remember, no tab key, no search results page. Just the key you
already know by heart.

WHY IT IS DIFFERENT

When what you type is not a URL, your browser starts a navigation towards your
search engine. Quick Jump rewrites that navigation before the request leaves your
machine. So no results page ever paints, and your internal project key never
reaches the search engine.

BUILT FOR SEVERAL JIRAS AT ONCE

Map as many keys as you like, each to its own instance. Self-hosted works
properly: a port and a path are both accepted, so intra.example.org/jira and
http://jira:8080 are fine.

IT SHOWS YOU WHERE YOU ARE GOING

Because the extension teaches you to stop reading the address bar, it shows the
destination everywhere else. Every shortcut displays its full destination, host
and path. Any change of destination raises a banner before your next jump, naming
the old and new host. A configuration file you import always arrives disarmed, and
its changes are shown side by side before anything is applied.

NOTHING LEAVES YOUR BROWSER

No account, no server, no analytics. The extension cannot make a network request
at all. Your shortcuts stay on your device unless you explicitly turn on syncing.

OPEN SOURCE

MIT licensed. Every release publishes checksums and a build provenance
attestation you can verify.
```

## Permission justifications

| Permission | Why it is needed | Why not more |
|---|---|---|
| `declarativeNetRequestWithHostAccess` | Rewrites the address-bar navigation to the issue URL before it is sent. | The plain `declarativeNetRequest` permission grants an ambient ability to act on traffic. `WithHostAccess` subordinates it to the hosts the user granted, which is exactly this extension's model. |
| `storage` | Keeps the user's shortcuts, engine selection and acknowledgements. | Local to the device by default; syncing is opt-in and states what it sends. |
| `optional_host_permissions: http://*/*, https://*/*` | Destinations are typed by the user, so they cannot be known at packaging time. | **Never requested in bulk.** Only concrete origins are requested, on a click, through the browser prompt that names them. |

**Absent on purpose**, and worth stating: no `tabs`, `scripting`, `webRequest`,
`webNavigation`, `history`, `cookies`, `downloads`, `clipboardRead` — and no
content script, so no code of this extension ever runs in a web page. No
`web_accessible_resources`, so a page cannot even detect that it is installed.
`declarativeNetRequestFeedback` is deliberately excluded: it would expose matched
navigation.

## Data disclosure

- Does it collect user data? **No.**
- Firefox `data_collection_permissions`: `["none"]`.
- Chrome data disclosure: no category checked; the certifications about not
  selling data, not using it for unrelated purposes and not using it for
  creditworthiness all apply.

Note for the reviewer: the extension declares `connect-src 'none'` on its pages
and contains no request API, so the absence of collection is verifiable from the
source rather than asserted.

## Graphics

| Asset | Size | Source |
|---|---|---|
| Icon | 128×128 | `src/icons/icon-128.png`, generated by `scripts/make-icons.mjs` |
| Screenshot | 1280×800 | Options page with two shortcuts, one of them self-hosted |
| Screenshot | 1280×800 | Import review showing a changed destination |
| Small promo tile | 440×280 | Optional |

## Checklist

- [ ] `npm test` and `npm run lint` clean
- [ ] `make icons` reports no drift
- [ ] Version bumped in `package.json` **and** `src/manifest.json` (`make bump`)
- [ ] Screenshots retaken if the UI moved
- [ ] Chrome Web Store developer fee paid (one-off, $5)
- [ ] `CWS_*` and `WEB_EXT_API_*` secrets set on the protected release environment
- [ ] Tag signed and pushed; provenance attestation published
- [ ] Trademark note present in the listing
